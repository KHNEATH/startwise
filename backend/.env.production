# Production Environment Variables for StartWise Backend
# Configure these in Vercel dashboard under Environment Variables

# Server Configuration
NODE_ENV=production
PORT=5001

# Database Configuration (Use PlanetScale or Railway MySQL)
DB_HOST=your-database-host
DB_USER=your-database-user
DB_PASSWORD=your-database-password
DB_NAME=startwise_db
DB_PORT=3306

# JWT Configuration
JWT_SECRET=your-super-secure-jwt-secret-for-production
JWT_EXPIRES_IN=24h

# CORS Configuration
FRONTEND_URL=https://your-frontend-project.vercel.app

# Security
BCRYPT_ROUNDS=12

# Database Pool Configuration
DB_CONNECTION_LIMIT=10
DB_QUEUE_LIMIT=0
DB_TIMEOUT=60000